<!DOCTYPE html>
<html>
  
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome to List Categorie !{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset ('css/bootstrap.css') }}">
</head>

<body>
<div class="container">
    <h1>Liste des catégories</h1>
        <button onclick="location.href = './categorie';"class="btn btn-primary"> 
            Ajouter une catégorie </button>

        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Nom</th>
                    <th scope="col">Editer</th>
                    <th scope="col">Supprimer</th>
            </thead>
            <tbody>
                {% for categorie in listCat %}
                <tr>
                    <th scope="row"> {{ categorie.id }} </th>
                    <td> {{ categorie.libelle }} </td>
                    <td><a href=updateCat?id_={{ categorie.id }}> Update </a></td> 
                    <td><a href=deleteCat?id_={{ categorie.id }}> Delete </a></td> 
                </tr>
                {% endfor %}
            </tbody>
        </table>

<button onclick="location.href = './listNote';" id="myButton" class="btn btn-link" >Retour à la liste des notes</button>

<div>


</body>

</html> 